# Environment variables — never commit these
.env
.env.local
.env.production

# SQLite database files
*.db
*.sqlite
*.sqlite3

# Scraper logs
*.log
logs/

# macOS
.DS_Store

# VS Code
.vscode/

# Node (if frontend tooling added later)
node_modules/

# Docker
.docker/

# Database files in data/ directories are excluded by the *.db rule above.
# backend/data/ itself is tracked so the directory exists in the repo (.gitkeep).

# Python
venv/
__pycache__/
*.pyc
.python-version

# Discovery tool outputs — review before merging into companies.yaml
backend/tools/discover_checkpoint.json
backend/tools/discovered_companies.yaml
backend/tools/discover_summary.txt
